--- a/server/src/utils/gameEngine.js
+++ b/server/src/utils/gameEngine.js
@@ -46,7 +46,7 @@ function shuffle(deck) {
   const d = [...deck];
   for (let i = d.length - 1; i > 0; i--) {
-    const j = Math.floor(Math.random() * (i + 1));
+    const j = randomInt(i + 1);
     [d[i], d[j]] = [d[j], d[i]];
   }
   return d;
}
